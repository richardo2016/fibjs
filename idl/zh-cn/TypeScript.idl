/*! @brief 全局的 TypeScript 对象, 该对象并非是 `require('typescript')` 的直接结果.
 */
interface TypeScript : object
{
    /*! @brief 构造一个新的 TypeScrip t对象
    */
    TypeScript();

    /*! @brief 查询当前内置 typescript 的版本 */
    readonly String version;
};
